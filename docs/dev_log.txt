Date:
2026-03-24

Task:
Completed the core server-rendered allocation workflow, rebalanced the demo dataset, removed dead placeholder assets, and tightened project logging, documentation, and automated test coverage.

What changed:
The buyer pledge homepage was reworked into a more compact aggregator queue with a selected-detail panel so the landing page now functions as a worklist rather than a dense set of cards. Status wording was simplified to presentation labels that are easier to scan, and the queue now supports row selection, filtering, sorting, and clearer prioritisation of active demand.

The allocation workflow was then promoted from placeholder templates into a real server-rendered flow. A new allocation builder page was connected to buyer pledges, eligible farm pledges are filtered by crop and buyer criteria, and a staging model was introduced so the aggregator can add or remove farm contributions before committing them. Submission is now wired through to the SQLite database, which means confirmed allocation rows are inserted into `pledge_allocations` and the related buyer and farmer pledge statuses are recalculated immediately after submission. The allocation progress bar was also refined so it distinguishes between already submitted quantities, currently staged quantities, and remaining unmet demand.

The synthetic dataset was rebalanced to make the prototype easier to use during demonstrations. Instead of a much larger and more repetitive dataset, generation is now centred on exactly 20 buyer pledges and a smaller supporting set of farmers, farmer pledges, allocations, and crop input logs. Buyer organisation names were replaced with spoofed, recognisable brand-inspired names to make the data easier to scan and remember. This reduced clutter on the buyer and allocation pages while still leaving enough variation to demonstrate workflow logic.

Placeholder files that were confirmed to have no role in the live Flask app were removed from the project. This included obsolete allocation placeholder templates, placeholder static assets, and the unused `home.html` template once `/` had been fully committed as a redirect to the buyer pledge queue. The repository structure now reflects the actual running prototype more closely.

Project-wide logging and test support were also tightened. Runtime logging configuration was updated so file logging is reliably configured to `logs/app.log`, route and database helper logging was expanded for allocation actions and setup/generation steps, the README was updated to match the current project structure and commands, `.gitignore` was expanded to cover local junk and pytest cache artefacts, and `pyproject.toml` now has stronger pytest configuration and explicit dev dependency support. A new database-helper test file was added so helper logic is now tested directly rather than only through route or end-to-end behaviour.

Why this was done:
These changes were needed to move the prototype from a collection of partially connected pages into a more coherent Version 0 workflow. The allocator-facing path now has a clear sequence: review demand, inspect eligible supply, stage contributions, and commit confirmed allocations. Reducing the dataset size was equally important because earlier volumes made the interface feel repetitive and harder to reason about during manual review.

Design reasoning:
The allocation workflow was deliberately kept server-rendered and session-backed rather than introducing a richer JavaScript state layer. This keeps the architecture consistent with the rest of the Flask prototype and makes state transitions easier to reason about while the core business logic is still being established.

The staging model creates a useful separation between temporary user choices and confirmed database state. This is an important architectural boundary because future export/report features and auditability depend on being able to distinguish between a working draft and a persisted allocation.

The additional database-helper tests were added because route tests alone are too coarse to validate derived values such as criteria summaries, deadline state classification, remaining quantity calculations, and bulk input-log retrieval. Testing those helper functions directly improves confidence in the application logic layer described in the report.

Problems encountered:
As the allocation builder became real, there was a risk of confusing staged session data with committed database data. This was addressed by making submission the only point at which database writes occur and by updating the allocation page to present staged content separately from already submitted quantities.

Another issue was that runtime logging still existed but had become too easy to misread as stale because configuration and coverage were inconsistent. The logging setup was revised so that shared handlers are configured more predictably and more route/helper events are written to the application log.

Solution applied:
Implemented the buyer queue redesign, added a live allocation builder route and supporting allocation logic, connected submission to the SQLite database, rebalanced the generated dataset around 20 buyer pledges, removed dead placeholder files, tightened logging configuration and coverage, updated project metadata/documentation, and added direct database-helper tests.

Files affected:
- `app.py`
- `database/generate_data.py`
- `database/setup_database.py`
- `docs/dev_log.txt`
- `logic/allocation_logic.py`
- `logic/database_helpers.py`
- `logic/logging_config.py`
- `pages/build_allocation.html`
- `pages/buyer_pledges.html`
- `pages/reusable/navbar.html`
- `pyproject.toml`
- `README.md`
- `.gitignore`
- `static/css/main.css`
- `tests/conftest.py`
- `tests/database/test_database.py`
- `tests/database/test_database_helpers.py`
- `tests/logic/test_allocation_logic.py`
- `tests/pages/test_allocations.py`
- `tests/pages/test_buyer_pledges.py`

Testing performed:
Ran:
- `./venv/bin/python database/setup_database.py`
- `./venv/bin/python database/generate_data.py`
- `./venv/bin/python -m pytest -q`

Results:
- database setup completed successfully,
- the demo database was regenerated around 20 buyer pledges,
- helper, route, logic, and database tests all passed,
- and the full suite passed with 53 tests.

Reflection:
This set of changes made the prototype more coherent as a coordination tool. The interface now better matches the intended aggregator workflow, the dataset is easier to work with, and the application logic is better supported by both runtime logging and direct automated tests. The project is also cleaner structurally because dead placeholder assets and stale documentation have been reduced, which should make the next stage of work easier to implement and explain.

Date:
2026-03-24

Task:
Implemented the aggregator-facing pledge and farm browsing workflow, including buyer pledge management, a simplified farm directory, farm profile drill-down, African coordinate-backed map display, and a more structured crop input log model.

What changed:
The homepage was simplified so that `/` now redirects directly to the buyer pledge management view. The buyer pledge page was redesigned from a plain listing into a clearer aggregator demand view with status-grouped sections for open, partially fulfilled, and fulfilled demand. The page now surfaces progress more intentionally, including fulfilment bars for partially fulfilled pledges, and reduces front-page clutter so the user can scan demand more quickly before drilling into later steps of the workflow.

The farm directory was also redesigned as a compact comparison table rather than a more information-heavy card grid. Filtering and sorting were added so an aggregator can search by farm name, farmer name, region, crop type, and near-term availability. This improves shortlist building by making the directory a scanning tool rather than a detail page.

Farm profiles were then extended into a more useful drill-down surface. Each farm profile now shows production pledges separately from the directory and includes a real Leaflet-based map widget driven by latitude and longitude stored in the database. The synthetic data generator and validation logic were updated so farm coordinates are realistic for supported countries in East and Southern Africa, with counties constrained to the correct country and coordinates constrained to country-specific bounds.

The crop-offer detail workflow was also introduced. Rather than showing generic recent logs at the bottom of the farm profile, each production pledge now links to its own crop-offer detail page where the pledge-specific input history is displayed. This made the compliance and traceability information more logically tied to the exact crop offer being reviewed.

Finally, the farm input log system was made more future-proof. Previously, logs only stored a broad `input_type`, which would not be sufficient for matching future buyer criteria such as required pesticides, banned fertiliser brands, or required application methods. The schema, synthetic data generator, retrieval helpers, and pledge detail page were updated so each log now stores a broad category together with more specific attributes: `product_name`, `brand_name`, and `application_method`. The generated data was also expanded substantially so crop offers now carry much richer input histories.

Why this was done:
These changes were needed to move the prototype closer to the actual aggregator workflow described in the report. The earlier pages technically displayed data, but they were too dense at the wrong level and not sufficiently structured for practical decision-making. The redesign separates scanning from inspection: the buyer pledge page is now for prioritising demand, the farm directory is for shortlisting suppliers, the farm profile is for reviewing a farm at a higher level, and the crop-offer detail page is where pledge-specific compliance context lives.

The input log redesign was especially important because the future allocation workflow depends on comparing buyer requirements with production history. A generic input label is not enough for that. Adding structured product, brand, and method fields creates a clearer path toward matching buyer criteria against actual production activity later in development.

Design reasoning:
The frontend changes follow a drill-down design principle. High-level pages should help the aggregator decide where to look next, not force them to process all available detail at once. This is why the buyer pledge page and farm directory were simplified while the pledge-specific crop detail page was made more informative.

The map is intentionally modest at this stage. Its current purpose is visual orientation and confirmation that location data is real and meaningful, not route optimisation or logistics analysis. However, storing realistic country-specific coordinates now creates a foundation for future distance or region-based allocation logic.

The updated input-log structure preserves the original broad `input_type` category while extending it with more exact identifiers. This is preferable to replacing the category entirely because future rules may need to match at different levels of specificity. For example, a buyer may forbid all pesticides at the category level, or forbid only a particular product or brand within that category.

Problems encountered:
The farm profile and crop-offer flow required a restructuring of where log history was displayed. This meant the earlier “recent input logs” section on the farm profile had to be removed to avoid duplicating information at the wrong level.

When the new structured input-log fields were added, a page-level test exposed a usability issue: if a crop offer had no logs yet, the detail page removed the table entirely, which meant the user could not still see the intended log structure. This was corrected by always rendering the table headers and using an empty-state row inside the table body when no records exist.

Solution applied:
Implemented server-side grouping and retrieval logic for buyer pledges, server-rendered filtering/sorting for farms, a dedicated farm pledge detail route, Leaflet map rendering on farm profiles, schema migration support for additional `farm_input_logs` fields, denser synthetic log generation, and expanded validation/tests to ensure the new structure remains consistent.

Files affected:
- `app.py`
- `database/setup_database.py`
- `database/generate_data.py`
- `logic/database_helpers.py`
- `pages/base.html`
- `pages/buyer_pledges.html`
- `pages/farms.html`
- `pages/farm_profile.html`
- `pages/farm_pledge_detail.html`
- `static/css/main.css`
- `tests/conftest.py`
- `tests/database/test_database.py`
- `tests/pages/test_home_page.py`
- `tests/pages/test_buyer_pledges.py`
- `tests/pages/test_farms.py`

Testing performed:
Ran:
- `./venv/bin/python database/setup_database.py`
- `./venv/bin/python database/generate_data.py`
- `./venv/bin/python -m pytest tests/database/test_database.py tests/pages/test_farms.py -q`
- `./venv/bin/python -m pytest -q`

Results:
- database schema migration completed successfully,
- regenerated demo data passed validation with zero anomalies,
- generated dataset included realistic African location data and substantially denser crop input histories,
- focused farm/database tests passed,
- and the full test suite passed with 33 tests.

Reflection:
This set of changes made the prototype behave more like a real coordination tool rather than a static data browser. The interaction model is now clearer: first prioritise demand, then shortlist farms, then inspect a farm, then inspect a specific crop offer and its input history. The structured input-log redesign is also an important architectural improvement because it anticipates future buyer-rule matching rather than locking the project into a weak logging model that would need to be redesigned later.

Date:
2026-03-24

Task:
Reworked the database setup, synthetic data generation, and database-focused testing into a single consolidated database workflow.

What changed:
Moved the database setup and synthetic data generation scripts into the `database` folder so the schema, generated database file, and generation utilities now live in one place. Removed the old split between `setup/` and `database/`, deleted the fixed `sample_data.sql` file, and removed the separate `create_tables.sql` file by embedding the schema directly in `database/setup_database.py`.

The generated test database is now built from the synthetic data generator rather than from a small static SQL seed file. This means the tests exercise the same setup and generation path used by the application database.

The test structure was also reorganised into grouped folders under `tests/`, and the template folder was renamed to `pages` with Flask updated to load templates from that directory.

Database testing was expanded significantly. The suite now checks:
- table creation,
- foreign key integrity,
- positive quantity rules,
- allocation totals relative to buyer demand,
- allocation totals relative to farmer supply,
- buyer pledge status correctness,
- presence of open, partial, and fulfilled pledge states,
- NULL coverage for optional fields,
- JSON validity in generated buyer notes,
- temporal consistency for pledge dates,
- and input log consistency with farmer/pledge relationships.

Why this was done:
The earlier structure contained redundant database assets spread across multiple places: SQL schema files, sample data files, setup scripts, and generated data paths. That made the project harder to understand and maintain. Consolidating the database workflow into one folder reduces duplication and makes it clearer how the prototype database is created, populated, and validated.

Design reasoning:
The schema is now defined in Python because the SQL file had become a single-purpose dependency only used by setup. Removing that external file simplified the project structure without changing database behaviour.

Using generated data for tests instead of a tiny fixed sample makes the database tests more representative of real usage. It also keeps the test path aligned with the actual data-generation workflow, which improves confidence that the setup and validation logic are working together correctly.

Problems encountered:
During consolidation, moving the setup script caused a temporary path collision and left `database/setup_database.py` missing. This was corrected by restoring the file directly in the target location and re-validating the workflow.

Solution applied:
Restored the setup script in `database/`, updated the generator to import from the new path, removed the obsolete `setup/` folder, and updated the README commands to match the new structure.

Testing performed:
Ran:
- `./venv/bin/python database/setup_database.py`
- `./venv/bin/python database/generate_data.py --buyers 8 --farmers 12 --max-buyer-pledges 3 --max-farmer-pledges 4 --max-input-logs 2 --seed 20260324`
- `./venv/bin/python -m pytest tests/database/test_database.py -q`
- `./venv/bin/python -m pytest -q`

Results:
- database setup succeeded,
- synthetic generation completed with zero anomalies,
- database test file passed with 13 tests,
- full test suite passed with 17 tests.

Reflection:
This change made the database layer more coherent and much easier to explain. The project now has a single database workflow: define schema, create database, generate realistic synthetic records, validate the result, and test against that same generation path. That is a stronger engineering foundation than maintaining separate schema files, sample data files, and one-off setup scripts that drift apart over time.

Date:
2026-03-24

Task:
Reviewed the starred placeholder files individually and restored them to normal filenames.

What changed:
Inspected each file that had previously been renamed to end with `**`, then renamed those files back to conventional source filenames. The reviewed files were:
- `logic/allocation_logic.py`
- `static/css/allocation.css`
- `static/js/allocation.js`
- `templates/allocation_summary.html`
- `templates/build_allocation.html`
- `templates/farm_profile.html`
- `templates/reusable/allocation_table.html`

Why this was done:
The starred naming convention created confusion about the current application state and made it harder to reason about whether the Flask app was broken because of bad file naming. The immediate priority was to separate the question of file naming from the question of actual application behaviour.

Design reasoning:
The correct test for a placeholder in this project is not whether a file is incomplete, but whether it currently contributes anything to the running app. Each starred file was checked individually against the active Flask routes, imports, and template references. A file was treated as a true placeholder only if it had no current effect on the website or application flow.

Findings:
All reviewed starred files are true placeholders at the moment.
- `logic/allocation_logic.py` contains only a placeholder module docstring and is not imported.
- `static/css/allocation.css` is not linked from any active template.
- `static/js/allocation.js` is not loaded by any active template and contains only a placeholder comment.
- `templates/allocation_summary.html`, `templates/build_allocation.html`, and `templates/farm_profile.html` are not wired to any route and only render “later version” content.
- `templates/reusable/allocation_table.html` is not included anywhere in the active template tree and only contains placeholder markup.

Problem encountered:
The earlier rename was visually explicit, but it introduced non-standard filenames that were more likely to distract from the actual issue than clarify it. In practice, the starred names did not identify a bug in the running app because these files were already unused.

Solution applied:
Restored normal filenames and documented the placeholder status in the development log instead of encoding that status into unusual file names. This keeps the codebase easier to work with while preserving traceability about which files are scaffolding only.

Testing performed:
Searched the repository for references to each reviewed file before renaming. The only references found were in the development log itself, confirming that the files do not currently participate in the active application.

Reflection:
This check clarified an important distinction: an incomplete file is not necessarily a placeholder, and a placeholder file is not necessarily the cause of application issues. The better approach is to assess whether a file is actually connected to the live route/template/import path before treating it as part of the current product surface.

Date:
2026-03-24

Task:
Renamed all placeholder implementation files so their filenames end with `**`.

What changed:
Renamed the current placeholder allocation-related files in the templates, static assets, and logic layer so that each filename now explicitly ends with `**`. This affected the placeholder allocation templates, the placeholder farm profile template, the reusable allocation table placeholder, the placeholder allocation JavaScript and CSS files, and the placeholder allocation logic module.

Why this was done:
This change was made to clearly mark files that are present in the repository as scaffolding rather than completed features. The aim was to make placeholder assets visually distinct from active implementation files during ongoing development.

Design reasoning:
Because these files are not part of the currently wired application flow, renaming them does not affect the live routes. Explicitly marking them in their filenames makes their status obvious when scanning the project structure and reduces the chance that unfinished files are mistaken for completed functionality.

Files affected:
- `templates/build_allocation.html**`
- `templates/allocation_summary.html**`
- `templates/farm_profile.html**`
- `templates/reusable/allocation_table.html**`
- `static/js/allocation.js**`
- `static/css/allocation.css**`
- `logic/allocation_logic.py**`

Problem encountered:
The requested naming convention is intentionally unusual for source files, particularly for template and Python module filenames. In a project with active imports or references to these paths, such a naming pattern could create tooling or usage friction.

Solution applied:
Applied the naming convention only to files that are currently placeholders and not wired into active routes or imports. This preserves the current application behaviour while satisfying the requirement to visibly mark placeholder files.

Testing performed:
Checked the repository for references to the renamed placeholder files before applying the change. No active references were found, so the rename did not require route, import, or asset-path updates.

Reflection:
This rename improves visual signalling in the repository, but it also introduces deliberately non-standard filenames. If these placeholder files are later promoted into active implementation, they should be renamed back to conventional source filenames as part of that feature work.

Date:
2026-03-24

Task:
Implemented a project-wide runtime logging system using Python's standard `logging` library.

What changed:
Added a shared logging configuration module in `logic/logging_config.py` and integrated it across the main Flask application, database helper functions, and the database setup workflow. The application now writes runtime logs to `logs/app.log` using a rotating file handler, while also sending log output to the console during development.

Why this was done:
The project needed a proper runtime logging system that was separate from the development log. The development log is for documenting implementation progress, technical reasoning, testing, and reflection. Runtime logging serves a different engineering purpose: it records application behaviour during execution so that requests, database access, setup operations, and future allocation workflows can be traced and debugged more effectively.

Design reasoning:
Python's built-in `logging` library was chosen because it is the standard and most widely used logging approach in Python applications. A shared configuration module was used so that logging behaviour is consistent across the project rather than being configured separately in multiple files. A rotating file handler was introduced to prevent the log file from growing indefinitely over time.

Files affected:
- `logic/logging_config.py`
- `app.py`
- `logic/database_helpers.py`
- `setup/setup_database.py`
- `README.md`
- `.gitignore`

Problem encountered:
The initial integration caused `python setup/setup_database.py` to fail because the shared logging configuration module could not be imported when the script was executed directly from the `setup` directory context. This happened because the project root was not automatically available on `sys.path` in that execution mode.

Solution applied:
Updated the setup script so that the project root is inserted into `sys.path` before importing the shared logging configuration. This restored compatibility with the documented setup command and allowed the logging system to be reused in the setup workflow without duplicating configuration logic.

Testing performed:
Ran the automated test suite using `./venv/bin/python -m pytest -q`, which passed successfully with 6 tests passing. Also ran `./venv/bin/python setup/setup_database.py` to confirm that the database setup still worked after the logging integration. Verified that `logs/app.log` was created and contained application and database log entries.

Changes to original plan:
An earlier attempt briefly created a separate development log file outside `docs`, but this was corrected so that the project development log remained in the `docs` folder while runtime execution logs were moved into the dedicated `logs` directory.

Reflection:
This change improved the project structure by separating two forms of logging that serve different purposes. The development log remains useful for academic reporting and design justification, while the runtime logging system now provides a maintainable technical foundation for debugging and tracing future features such as allocation logic, supply aggregation, and database-driven coordination workflows.
